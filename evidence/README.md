# Evidence

This directory holds screenshots, curl outputs, and other evidence captured incrementally during development. Evidence is referenced from DOCUMENTATION.md Section 7.

Evidence will be captured at these steps:
- After signup: curl command + users table screenshot showing stored hash
- After verification: verification code before/after expiry
- After reset: reset token single-use evidence
- After rate limiting: 429 response evidence
